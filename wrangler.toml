name = "vedarublog-audio-proxy"
main = "worker.js"
compatibility_date = "2026-01-01"

# If you prefer to set account_id here, uncomment and set your account id
# account_id = "your_account_id_here"

[vars]
TARGET_ORIGIN = "https://m801.music.126.net"

# Use `wrangler publish` to deploy using the CLOUDFLARE_API_TOKEN secret in CI or your local environment.
# Example local deploy: export CLOUDFLARE_API_TOKEN=your_token && wrangler publish
