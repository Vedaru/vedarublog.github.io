declare module "astro-icon/components" {
  // Minimal typings for astro-icon subpath used by this project.
  // `Icon` is an Astro component exported by the package. Using `any` to avoid stricter ASTRO type dependencies.
  export const Icon: any;
  export default Icon;
}
